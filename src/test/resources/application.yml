# 스프링의 경우 별도의 설정이 없으면 메모리 모드로 돌림
spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        # system.out에 출력을 함
#        #show_sql: true
#        format_sql: true
logging:
  level:
    # 로거를 통해 출력
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace
